name: Build and Deploy Node.js app to GitHub Pages

on:
  push:
    branches: ["master"]   # 如果你的默认分支是 main，就改成 ["main"]

  workflow_dispatch:        # 支持在 Actions 页面手动触发

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # 配好 Node.js 环境
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22.19.0        # 你要用的 Node 版本
          cache: npm              # 如果用 pnpm/yarn，这里要改

      # （可选）配置 GitHub Pages 环境
      - name: Setup Pages
        uses: actions/configure-pages@v5

      # 安装依赖
      - name: Install dependencies
        run: npm install
        # 如果你本地一直是 npm install，也可以改成：npm install

      # 构建静态文件
      - name: Build
        run: npm run build

      # 上传构建产物，后面 deploy job 会用到
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./dist           # 📌 如果你的输出目录不是 dist，改成对应目录

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    runs-on: ubuntu-latest
    needs: build

    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
